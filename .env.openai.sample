# OpenAI 兼容 API 配置示例（如 LM Studio）
API_TYPE=openai
API_URL=http://localhost:1234/v1
OPENAI_MODEL=qwen/qwen3-1.7b

# 模型采样参数配置
# TEMPERATURE: 控制输出随机性，值越高结果越随机
# TOP_P: 控制核采样，值越高结果越多样化
# 推荐: TEMPERATURE=0.2, TOP_P=0.5
TEMPERATURE=0.2
TOP_P=0.5